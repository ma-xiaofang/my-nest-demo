Chain（固定流程）：
  用户 → 步骤1 → 步骤2 → 步骤3 → 结束
  流程提前写死，模型只负责生成内容

Agent（自主决策）：
  用户 → 模型思考 → 决定用哪个工具 → 调用工具 → 看结果 → 继续思考 → 最终回答
  模型自主决定调用哪个工具、调用几次，最终给出答案
  
比喻：
  Chain = 工厂流水线（固定工序）
  Agent = 聪明的员工（自己决定做什么）